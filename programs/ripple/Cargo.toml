[package]
name = "Rivo"
version = "0.1.0"
description = "Rivo — minimal on-chain purchase rail for creator payments (MVP)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "Rivo"

[features]
default = []

[dependencies]
anchor-lang = "0.31.1"
