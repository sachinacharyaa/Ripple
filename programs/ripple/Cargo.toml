[package]
name = "ripple"
version = "0.1.0"
description = "Ripple — minimal on-chain purchase rail for creator payments (MVP)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "ripple"

[features]
default = []

[dependencies]
anchor-lang = "0.31.1"
